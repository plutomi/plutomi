{
  "name": "plutomi",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build",
    "dev": "npm run build && nx run @plutomi/web:dev"
  },
  "private": true,
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.8.9",
    "prettier": "^2.6.2",
    "typescript": "^5.0.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
