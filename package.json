{
  "name": "plutomi",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "nx run-many -t=build",
    "build:fresh": "nx run-many -t=build --skip-nx-cache",
    "dev": "nx run-many -t=dev"
  },
  "keywords": [
    "plutomi"
  ],
  "author": "Plutomi Inc.",
  "license": "Apache2-0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@nrwl/nx-cloud": "^15.3.2",
    "nx": "^15.8.9",
    "prettier": "^2.8.7"
  }
}
