/**
 * These totals are for an org, across all workspaces!
 */
export type OrgTotals = {
  applicants: number;
  applications: number;
  users: number;
  stages: number;
  webhooks: number;
  questions: number;
  workspaces: number;
};
