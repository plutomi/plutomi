// In cents, per applicant
export const ApplicantPrices = {
  idle: 10, // per month
  active: 50, // Per month
  deletion: 25,
  frozen: 100,
};

export const StageBackgroundColors = {
  idle: `bg-blue-gray-100`,
  active: `bg-orange-100`,
  frozen: `bg-cyan-100`,
  deletion: `bg-rose-100`,
};

export const StageTextColors = {
  idle: `text-blue-gray-600`,
  active: `text-orange-600`,
  frozen: `text-cyan-600`,
  deletion: `text-rose-600`,
};
