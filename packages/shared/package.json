{
  "name": "@plutomi/shared",
  "version": "0.1.0",
  "description": "Shared Package for Plutomi. Contains things like types & utils used across packages.",
  "repository": "https://github.com/plutomi/plutomi",
  "author": "Plutomi Inc.",
  "license": "Apache-2.0",
  "dependencies": {
    "@ngneat/falso": "^7.1.0",
    "nanoid": "3.3.4"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc --project tsconfig.build.json",
    "lint": "yarn eslint --ext .ts,.tsx .",
    "lint:fix": "yarn eslint --ext .ts,.tsx --fix ."
  },
  "devDependencies": {
    "rimraf": "^4.4.1",
    "ts-node-dev": "^2.0.0"
  }
}
