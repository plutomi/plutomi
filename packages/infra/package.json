{
  "name": "@plutomi/infra",
  "version": "0.1.0",
  "scripts": {
    "cdk": "cdk",
    "test": "jest",
    "lint": "yarn eslint --ext .ts,.tsx .",
    "lint:fix": "yarn eslint --ext .ts,.tsx --fix .",
    "deploy:dev": "cdk deploy --require-approval any-change --progress events --verbose --profile=plutomi-dev",
    "deploy:stage": "cdk deploy --require-approval any-change --progress events --verbose --profile=plutomi-stage",
    "deploy:prod": "cdk deploy --require-approval any-change --progress events --verbose --profile=plutomi-prod"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "eslint": "^8.38.0",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@plutomi/env": "^0.1.0",
    "aws-cdk-lib": "^2.88.0",
    "cdk-fck-nat": "^1.0.93",
    "constructs": "^10.0.0",
    "dotenv": "^16.0.3",
    "source-map-support": "^0.5.21"
  }
}
