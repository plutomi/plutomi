on: [push, workflow_dispatch]

jobs:
  STAGING:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: '16' # TODO bump this up

      - name: Install dependencies
        run: npm i && npm i aws-cdk -g #TODO clean this up

      - name: Print destination
        run: echo "Deploying to staging environment"

      - name: Configure aws credentials
        uses: aws-actions/configure-aws-credentials@master
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Logging in to Docker
        run: npm run docker-fix

      - name: Deploy stack
        run: cdk deploy staging-DynamoDBStack staging-StreamProcessorStack staging-AppStack staging-EventBridgeStack staging-StorageStack staging-DeleteChildrenMachineStack staging-AthenaDynamoQueryStack staging-WebhooksMachineStack --verbose --require-approval never
        env:
          COMMITS_TOKEN: ${{ secrets.COMMITS_TOKEN }}
          HOSTED_ZONE_ID: ${{ secrets.HOSTED_ZONE_ID }}
          ACM_CERTIFICATE_ID: ${{ secrets.ACM_CERTIFICATE_ID }}
          LOGIN_LINKS_PASSWORD: ${{ secrets.LOGIN_LINKS_PASSWORD }}
          SESSION_SIGNATURE_SECRET_1: ${{ secrets.SESSION_SIGNATURE_SECRET_1 }}
          NODE_ENV: production # TODO change
          DEPLOYMENT_ENVIRONMENT: staging
          NEXT_PUBLIC_BASE_URL: staging.plutomi.com
          MONGO_CONNECTION: NOT_SET_NEEDS_OTHER_PR
